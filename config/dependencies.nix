{
    dependencies = {
      # Pedidos por tree-sitter:
      tree-sitter.enable = true;
      gcc.enable = true;

      # Por si las moscas:
      coreutils.enable = true;
      git.enable = true;
    };
}